apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: per-custom-model
spec:
  predictor:
    model:
      modelFormat:
        name: custom-per
      storageUri: s3://per-demo-sep
